<!DOCTYPE html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width, initial-scale=1.0" />
		<title>Node simple server</title>
		<link
			href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
			rel="stylesheet"
			integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH"
			crossorigin="anonymous"
		/>
	</head>
	<body>
		<%- include('./partials/user-header') %>

		<div class="vh-100">
			<div class="container">
				<table
					class="table table-striped table-hover table-bordered border-primary text-center mt-4"
					id="table"
				>
					<thead>
						<tr>
							<th scope="col">ФИО</th>
							<th scope="col">Номер телефона</th>
							<th scope="col">Описание проблемы</th>
							<th scope="col">Дата и время заявки</th>
						</tr>
					</thead>
					<tbody>
						<% if (applications.length) { for (let i = 0; i <
						applications.length; i++) { %>

						<tr
							id="<%= applications[i].id %>"
							data-bs-toggle="tooltip"
							title="Нажмите для удаления"
						>
							<th scope="row"><%= applications[i].username %></th>
							<td><%= applications[i].phonenumber %></td>
							<td><%= applications[i].message %></td>
							<td>
								<%= new Date(applications[i].createdAt).toISOString().slice(0,
								16).replace('T', ' ') %>
							</td>
						</tr>

						<% } } %>
					</tbody>
				</table>
			</div>
		</div>
		<script src="/app.js"></script>
	</body>
</html>
